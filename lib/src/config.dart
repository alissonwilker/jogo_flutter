import 'package:flutter/material.dart';

// dimensoes
const gameWidth = 500.0;
const gameHeight = 250.0;
const notaRaio = gameWidth * 0.02;
const hitPositionX = gameWidth / 4;

// cores
const jogoBackgroundColor = Color.fromARGB(255, 242, 232, 207);
const hitColor = Color.fromARGB(255, 30, 145, 70);
const notaColor = Color.fromARGB(255, 30, 96, 145);
const playAreaColor = Color.fromARGB(255, 183, 239, 238);

// chaves
const hitKey = 'hitKey';
